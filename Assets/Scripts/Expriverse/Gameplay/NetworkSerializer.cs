namespace Expriverse.Gameplay {

    public static class NetworkSerializer {

        //public static void WriteValueSafe(this FastBufferWriter writer, Source _source) {
        //    writer.WriteValueSafe(_source != null ? _source.entity.Id : ulong.MaxValue);
        //}

        //public static void ReadValueSafe(this FastBufferReader reader, out Source _source) {
        //    reader.ReadValueSafe(out ulong _id);
        //    Source.IdToAttacker.TryGetValue(_id, out _source);
        //}

        //public static void WriteValueSafe(this FastBufferWriter writer, Victim victim) {
        //    writer.WriteValueSafe(victim != null ? victim.entity.Id : ulong.MaxValue);
        //}

        //public static void ReadValueSafe(this FastBufferReader reader, out Victim victim) {
        //    reader.ReadValueSafe(out ulong _id);
        //    Victim.IdToVictim.TryGetValue(_id, out victim);
        //}

        //public static void WriteValueSafe(this FastBufferWriter writer, Healer healer) {
        //    writer.WriteValueSafe(healer != null ? healer.entity.Id : ulong.MaxValue);
        //}

        //public static void ReadValueSafe(this FastBufferReader reader, out Healer healer) {
        //    reader.ReadValueSafe(out ulong _id);
        //    Healer.IdToHealer.TryGetValue(_id, out healer);
        //}

        //public static void WriteValueSafe(this FastBufferWriter writer, Target target) {
        //    writer.WriteValueSafe(target != null ? target.entity.Id : ulong.MaxValue);
        //}

        //public static void ReadValueSafe(this FastBufferReader reader, out Target target) {
        //    reader.ReadValueSafe(out ulong _id);
        //    Target.IdToTarget.TryGetValue(_id, out target);
        //}
    }
}
